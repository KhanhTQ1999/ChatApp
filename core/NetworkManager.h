#ifndef NETWORKMANAGER_H
#define NETWORKMANAGER_H

class NetworkManager {
public:
    NetworkManager();
    ~NetworkManager();

    NetworkManager(const NetworkManager&) = default;
    NetworkManager& operator=(const NetworkManager&) = default;

private:

};

#endif // NETWORKMANAGER_H